name: example11
on: [workflow_dispatch]
jobs:
  Stage1:
    runs-on: ubuntu-latest
    steps:
      - run: touch new.txt
      - run: ls -l
  Stage2:
    runs-on: ubuntu-latest
    steps:
      - run: ls -l
